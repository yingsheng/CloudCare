package agent;
public class agent {
	//input: user string
	//output: see design doc.
	
	// step.1
	//call parser, use the ArrayList<String> of st.symptom: 1 output to user, 2.pass it to diagnoser
	
	// step. 2
	//call diagnoser
	
	// step.3
	//call asker if disease list return by diagnoser is too long

	// step.4
	//accept asker's s.t. symptom, generate question for user
	
	//accept user's "yes/no" input, call asker.
}
